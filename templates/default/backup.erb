#!/bin/sh

REPOZO="<%= node[:zeoserver][:virtualenv] %>/bin/repozo"
DATAFS="<%= node[:zeoserver][:dir_filestorage] %>/Data.fs"
BACKUP_LOCATION="<%= node[:zeoserver][:dir_backups] %>/backups"

exec "$REPOZO" "--backup" "-f" "$DATAFS" "-r" "$BACKUP_LOCATION" "--gzip"
